
name: karaoke_app
description: Glossy karaoke app UI matching the supplied 8-screen reference.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  file_picker: ^10.3.3
  audioplayers: ^6.5.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
